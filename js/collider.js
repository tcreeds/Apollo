function Collider( position, axes, halfWidths, vertices, transform ){
        this.position = position;
        this.axes = axes;
        this.halfWidths = halfWidths;
        this.vertices = vertices;
        this.transform = transform;
}